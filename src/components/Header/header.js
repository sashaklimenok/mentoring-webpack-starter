import "./header.scss";
import Swiper from "swiper";

export const header = () => {
  const sum = 1000 + 2342213;

  return sum + "Im Header111";
};

const swiper = new Swiper();

console.log(header());
